const config = {
  initialMessages: [
    // Initial messages to be displayed in the chatbot
  ],
  // Other configuration options (e.g., customStyles, customComponents, etc.)
};

export default config;
